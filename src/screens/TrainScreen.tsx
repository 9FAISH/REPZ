import { PhaseStub } from '../components/PhaseStub'

export function TrainScreen() {
  return (
    <PhaseStub
      title="Push A"
      note="Slot-based workout builder lands in Phase 3 — one exercise per muscle slot, filtered by your equipment."
    />
  )
}
